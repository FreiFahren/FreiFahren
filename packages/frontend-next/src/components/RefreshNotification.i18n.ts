import { i18n } from '@/lib/i18n';

export const NAMESPACE = 'refreshNotification';

i18n.addResourceBundle('en', NAMESPACE, {
  refreshed: 'Reports updated',
});

i18n.addResourceBundle('de', NAMESPACE, {
  refreshed: 'Meldungen aktualisiert',
});
